package com.bkomendera.earthquakes;

public class EarthquakesServiceTests {
/*
    @Mock
    EarthquakesRepository earthquakesRepository;
    @Mock
    EarthquakesApiImpl earthquakesApiInterface;

    @BeforeAll
    void static onInit(){
        MockitoAnnotations.initMocks();
    }

    @Test
    @DisplayName("should save data to the repository")
    void repoSaveTest() throws IOException {
        Map<String, Point2D> map = new HashMap<>();
        earthquakesApiInterface.getAllMonth().forEach(feature -> map.put(feature.getProperties().getTitle(),
                new Point2D.Float(feature.getGeometry().getCoordinates().get(0), feature.getGeometry().getCoordinates().get(1)))
        );
        earthquakesRepository.saveEarthquakesRepo(map);
        Assertions.assertFalse(earthquakesRepository.getEarthquakesRepo().isEmpty());
        System.out.println("Example data in repo: " + earthquakesRepository.getEarthquakesRepo().get(0));
    }*/

}
